[package]
name = "sp-annotate"
version = "0.1.0"
edition = "2021"

[dependencies]
pdf_oxide = "0.3"
sp-check = { path = "../sp-check" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
