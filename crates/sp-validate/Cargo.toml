[package]
name = "sp-validate"
version = "0.1.0"
edition = "2021"

[dependencies]
sp-extract = { path = "../sp-extract" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
regex = "1"
